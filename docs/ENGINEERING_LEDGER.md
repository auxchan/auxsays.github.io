# AUXSAYS Engineering Ledger

The single canonical record of resolved engineering defects, known residual risks, deferred
hypotheses and architectural decisions that may need revisiting.

This is an **index for future diagnosis**, not a forensic record. Entries are short on purpose. The
commit, PR and test named in each entry hold the detail.

**Do not create competing known-issues, audit, decision or residual documents for routine
engineering work.** Everything of that kind belongs here.

Working rule for what blocks a lane and what gets ledgered: see *Progress-first triage* in
[`AGENTS.md`](../AGENTS.md).

| ID | Status | Area | Severity | Title |
|---|---|---|---|---|
| AUX-001 | Resolved | Patch decision | High | One patch published two contradicting decisions |
| AUX-002 | Resolved | Evidence projection | High | DaVinci projections lagged their own count |
| AUX-003 | Resolved | Ingestion health | High | A dead source reported healthy |
| AUX-004 | Resolved | Presentation | Medium | Patch headings invisible in OS light mode |
| AUX-005 | Deferred | Patch decision | Medium | Official hard-stop AVOID path (partially built) |
| AUX-006 | Deferred | Patch decision | Medium | Community-triggered AVOID |
| AUX-007 | Deferred | Patch decision | Medium | OBS verdict keys on a dead `update_status` axis |
| AUX-008 | Deferred | Vocabulary | Low | Verdict vocabulary declared five ways |
| AUX-009 | Deferred | Deploy gate | Low | Conflicting-actions guard cannot block a publish |
| AUX-010 | Deferred | Presentation | Medium | Site-wide heading contrast |
| AUX-011 | Deferred | Classification | Medium | Severity classifier matches unnegated substrings |
| AUX-012 | Deferred | Coverage | Low | Premiere 26.2 is regenerated by no lane |
| AUX-013 | Deferred | CI governance | Medium | CI runs but does not gate merges |
| AUX-014 | Deferred | Presentation | Low | Security-advisory branch is dead by key-path mismatch |

---

## Resolved

### AUX-001 — One patch published two contradicting decisions

- **Status** Resolved · **Area** Patch decision · **Severity** High
- **First seen** 2026-09-16 · **Resolved** 2026-09-16
- **PR** #142 · **Merge** `bc5d1a19` · **Production** `962f211c`

**Symptom** `2026-04-14-davinci-resolve-21-public-beta-1.md` published `WAIT for production systems`
as its verdict and `AVOID for production` as its summary. Both reached readers — the patch page
renders the verdict, the `/updates/` feed renders the summary — so no single page showed the
conflict. 10 of 1,168 records named more than one action.

**Root cause** Three independent implementations of one install/wait/avoid policy:
`_record_coherence_fields` (product policy), `_public_summary` → `_recommendation_prefix`
(sentiment, with its own beta rule), and `build_consensus_from_evidence.recommendation_prefix`.

**Resolution** `lib/patch_decision.py` is the only place a decision is chosen.
`_proposed_record_fields` calls it once per record and hands the same string to both writers.
`_recommendation_prefix` deleted, not delegated. Also closed: the two paths judged different
version strings (`ver` vs `patch_display_label(...)`).

**Verification** 212 evidence groups A/B'd, baseline writer vs patched: 9 groups changed, all only
in `update_consensus_summary`, all already self-contradicting; records naming two actions 9 → 0.
CI 89 suites / 4,996 checks. Production regeneration repaired 9 records; `qa_patch_records` went
9 warnings → 0 across 1,168 records; both live surfaces verified in agreement.

**Known residuals** AUX-006, AUX-007, AUX-008, AUX-009 all originate here.

**Reopen only if** conflicting verdict actions appear on a record again; a second recommendation
authority is introduced; or production regeneration produces divergence between the record writer
and `build_consensus_from_evidence`.

**Evidence** `auxsays/scripts/tests/test_patch_decision_authority.py` (47 governed checks, 7
mutants); `qa_patch_records.record_names_conflicting_actions`.

---

### AUX-002 — DaVinci projections lagged their own count

- **Status** Resolved · **Area** Evidence projection · **Severity** High
- **Resolved** 2026-09-15 · **PR** #141 · **Merge** `66fc0f06`

**Symptom** 8 of 120 DaVinci records published a count their own source list and headline
contradicted; 16 `qa_patch_records` warnings.

**Root cause** PR #69 narrowed in-collector writes to two fields and delegated projections to "a
later authority". DaVinci's only promotion lived in a `workflow_dispatch`-only workflow that had
never run in production, while the scheduled lane ran an unscoped reconcile that moved the count.

**Resolution** Scoped DaVinci promotion step added to the scheduled lane, positioned after
reconciliation and before the first QA.

**Verification** Warnings 16 → 0; all 120 DaVinci records coherent; DaVinci 21 moved 57/57/57 →
58/58/58 atomically in production.

**Reopen only if** a product's count moves in a lane that does not also rebuild its projections.

---

### AUX-003 — A dead source reported healthy

- **Status** Resolved · **Area** Ingestion health · **Severity** High
- **Resolved** 2026-09-11 · **PRs** #136, #137, #140 · **Merge** `cf8885bf`

**Symptom** Netlify rendered publicly as `Active / No new records` while its extractor mapped
nothing upstream.

**Root cause** Health was inferred from "no rows extracted", which is indistinguishable from a
genuinely quiet source.

**Resolution** A source with upstream content present but a structural parser/extractor mismatch is
classified broken immediately; historical extraction bootstraps from `seen`; a newly enabled source
keeps a bounded grace period.

**Verification** Production N+1 proved state, annotations, run verdict and public presentation
agree. Mixed healthy+impaired fleet stays degraded at exit 0.

**Known residuals** Partial rot (a source that extracts *some* of what it should) is still not
detected.

**Reopen only if** a source with a structurally broken extractor presents as healthy again.

---

### AUX-004 — Patch headings invisible in OS light mode

- **Status** Resolved (patch pages only) · **Area** Presentation · **Severity** Medium
- **Resolved** 2026-09-11 · **PR** #139 · **Merge** `b50fe727`

**Symptom** Heading colour followed the OS theme while the AUXSAYS ground stays fixed-dark, giving
~1.3–1.36:1 contrast on every patch page.

**Resolution** `body.aux-update { --heading-color: var(--text-0); }`.

**Known residuals** Every other layout still has it — see AUX-010.

**Reopen only if** patch-page headings regress below 4.5:1 in either OS theme.

---

## Deferred

### AUX-005 — Official hard-stop AVOID path (partially built)

- **Status** Deferred · **Area** Patch decision · **Severity** Medium · **First seen** 2026-09-23
- **Branch** `sprint/official-hard-stop-avoid` · **WIP commit** `9fdbcbdb` (not merged, not a PR)

**Symptom / gap** AVOID remains a canonical AUXSAYS verdict but is currently unreachable from
`decision_label()`, so the product cannot answer "avoid" even when a vendor withdraws a build.

**Approved policy** Production AVOID uses **official hard-stop semantics only**: the exact
patch/build was withdrawn or recalled, the vendor explicitly says do not install it, or the vendor
explicitly recommends rollback/uninstall over a serious regression. Not inferred from ordinary
known issues, sentiment, report count, classifier severity, issue theme, beta status or generic
safeguard language. A cohort-scoped safeguard hold must **not** globally convert a patch to AVOID;
holds stay as structured official risk evidence.

**State of the work** Landed on the branch: `patch_decision.official_hard_stop()` reading a
structured `official_install_block` (fails closed on non-dict, non-`active`, unrecognised `type`,
or any scope that is not explicitly `global`), AVOID placed above the count check, a
product-agnostic hard-stop branch in `_record_coherence_fields` that survives a zero community
count, and a hard-stop summary. **Not done:** threading the state through
`build_consensus_from_evidence.record_status_index` so all three call sites agree; retraction-fence
protection (`ZERO_COUNT_PROJECTION_FIELDS` deletes `update_decision_label` at zero count); the
layout's `official_only_zero_reports` override, which currently forces `INSUFFICIENT DATA` and
would erase an AVOID at zero count; a QA exemption; tests; governed manifest count.

**Known residual (design)** The promotion work-list is built from evidence rows, so a record with a
hard stop and zero community reports is never visited by the writer. Closing that needs the
work-list to union in records carrying an active block.

**Why deferred** No producer of `official_install_block` exists, no tracked build is currently
withdrawn, and the expected corpus outcome is 0 AVOID records. Nothing is broken in production.

**Current impact** None. AVOID is unreachable; no record claims otherwise.

**Revisit trigger** A vendor withdraws, recalls or issues a do-not-install/rollback directive for a
tracked build; or a producer for the structured field is proposed.

**Must not block** Any roadmap work. This is additive.

---

### AUX-006 — Community-triggered AVOID

- **Status** Deferred · **Area** Patch decision · **Severity** Medium

**Gap** Community evidence cannot emit AVOID and will not for this implementation generation.

**Why deferred** The limitations are structural, not a missing threshold: `severity: critical` is
classifier-derived; severity, issue theme and sentiment are returned by a single classifier branch,
so conjoining them is one lexical match counted three times rather than independent corroboration;
five of seven products have no severity pathway at all; 184 of 202 evidence-bearing patches have a
single source family; and corroborated critical evidence arrives a median 25 days after release, by
which time the patch is usually superseded.

**Current impact** None. AVOID is simply not community-reachable.

**Revisit trigger** All of: structured harm classification, trustworthy severity extraction,
cross-product support, independent corroboration, adequate source-family depth.

**Must not block** Any roadmap work. Do not invent a community threshold in the meantime.

---

### AUX-007 — OBS verdict keys on a dead `update_status` axis

- **Status** Deferred (approved for a later policy PR) · **Area** Patch decision · **Severity** Medium

**Symptom** `decision_label` returns `WAIT` for an archived OBS build and `TEST FIRST` otherwise.
`update_status: archived` has exactly one writer in the repo — `fetch_obs_updates.py:124`, which
marks the runner-up of the top **two** GitHub releases — and its workflow is named
*OBS Updates Consensus Pilot Deprecated*, is dispatch-only, and states the script is retained for
reference only. 15 of 16 OBS records say `current` and nothing will re-archive them.

**Consequence** OBS 32.1.1 (43 reports) publishes WAIT while the newer 32.1.2 (97 negative reports)
publishes TEST FIRST, purely from a stale positional flag.

**Approved direction** OBS verdict becomes evidence-driven; archived/current is removed as a
decision axis.

**Current impact** Reader-visible but not incorrect against stored data.

**Revisit trigger** The next patch-decision policy PR.

**Must not block** Roadmap work outside patch-decision policy.

---

### AUX-008 — Verdict vocabulary declared five ways

- **Status** Deferred (approved for a later policy PR) · **Area** Vocabulary · **Severity** Low

**Symptom** The vocabulary is declared in `AGENTS.md`, `_includes/patch-table-row.html`,
`assets/js/patch-table-sort.mjs`, `lib/patch_decision.DECISIONS` and `qa_patch_records`, and no two
agree. `AGENTS.md` lists three labels the authority cannot produce and omits `TEST FIRST` and
`WAIT for production systems`, which are live stored values.

**Approved direction** Normalize to a canonical eight-verdict vocabulary; declare one source
normative and generate the others from it.

**Current impact** Contributor-facing confusion only.

**Must not block** Roadmap work.

---

### AUX-009 — Conflicting-actions guard cannot block a publish

- **Status** Deferred (approved for a later policy PR) · **Area** Deploy gate · **Severity** Low

**Symptom** `pages.yml` runs `qa_patch_records.py` before `jekyll build`, but
`record_names_conflicting_actions` is a **warning** and `main()` exits non-zero only on errors, so a
record naming two conflicting actions can still publish.

**Why it is a warning** The same script runs in the scheduled lane after the promotions and before
the writeback, where an error would wedge the lane and discard every product's evidence for that
cycle.

**Approved direction** Conflicting actions block Pages after writeback, while remaining
warning-level during repair/writeback.

**Current impact** None today — the corpus is at 0 conflicts.

**Revisit trigger** The next patch-decision policy PR.

---

### AUX-010 — Site-wide heading contrast

- **Status** Deferred (approved, not started) · **Area** Presentation · **Severity** Medium

**Symptom** The AUX-004 defect remains on every layout except patch pages: home 7/7 headings, feed
1154/1154, vendor 4/6, product 2/4, methodology 13/13, about 1/3, at ~1.30–1.36:1 in OS light mode.

**Why deferred** Sequenced behind patch-decision policy work by directive.

**Must not block** Nothing blocks it now except sequencing.

---

### AUX-011 — Severity classifier matches unnegated substrings

- **Status** Deferred · **Area** Classification · **Severity** Medium

**Symptom** `patch_collectors/microsoft_windows.py:classify()` tests substrings without negation
handling, and returns theme, workflow area, platform, severity and sentiment from one branch. A
stored row titled *"Windows 11 Freeze - NO BSOD - NO CRITICAL EVENT ERROR"* carries
`severity: critical`, `issue_theme: BSOD / stop error`, `counted: true`.

**Current impact** Contained. Severity and issue theme do not feed any verdict, and under AUX-005
AVOID does not read them. The affected row still counts toward a report total, as any report does.

**Revisit trigger** Any proposal to let severity or issue theme influence a decision — which is
also the AUX-006 dependency.

**Must not block** Roadmap work.

---

### AUX-012 — Premiere 26.2 is regenerated by no lane

- **Status** Deferred (intentional) · **Area** Coverage · **Severity** Low

**Symptom** `2026-04-30-premiere-pro-26-2.md` stores `WAIT for production systems` in its label and
`WAIT` in its summary — the same action in two phrasings.

**Why deferred, deliberately** The record carries hand commits; the scheduled lane states Premiere
"is deliberately absent and must stay absent" because promoting it would replace human decision
prose with generated text (PR #77). The corpus guard compares actions rather than phrasings, so it
correctly stays silent on this record.

**Must not block** Anything.

---

### AUX-013 — CI runs but does not gate merges

- **Status** Deferred · **Area** CI governance · **Severity** Medium

**Symptom** The integrity workflow runs 89 suites on every PR and fails on check-count drift, but
`main` is not branch-protected (`branches/main/protection` → 404, rulesets `[]`), so a red run
blocks nothing. Two cited suites also never run: the JS rank test is `environment_specific`, and
`test_davinci_promotion_scope.py` is `mutating`.

**Why deferred** Previously investigated and blocked on dedicated app identity: a personal repo
cannot name the Actions bot as a bypass actor, and RepositoryRole bypass inverts the policy.

**Current impact** Discipline-dependent, not automatic.

**Revisit trigger** A red CI run is merged, or the repo gains an org/app identity.

---

### AUX-014 — Security-advisory branch is dead by key-path mismatch

- **Status** Deferred · **Area** Presentation · **Severity** Low

**Symptom** `aux-update.html` computes `security_sensitive` from four top-level keys, but 390
records carry `source_type: security_advisory` **nested** inside their sources, so the security
wording branch never fires.

**Current impact** A security-sensitive zero-report record shows the generic "Too few reports"
body instead of the security-specific one.

**Revisit trigger** Any work on patch-page decision presentation.

**Must not block** Roadmap work.

---

## Adding an entry

Keep it to the fields above. Resolved entries need **Reopen only if** with objective conditions.
Deferred entries need **Why deferred**, **Current impact**, **Revisit trigger** and **Work that this
item must not block**. If an entry is growing past roughly a screen, the detail belongs in the PR
body or a test docstring, not here.
