# What Changed UX

Status: LOCAL IMPLEMENTATION — HUMAN QA PENDING

The persistent world now includes a compact graph-linked `What changed` surface with `Recent`, `24h`, `7d`, `30d`, `90d`, and `1y` filters.

Each entry shows event class, headline, plain-English summary, represented period, official source, and original evidence when available. Selecting an entry navigates the same persistent world to its mapped factor; it does not open a disconnected dashboard.

Operational source health is visually and semantically separate from economic impact. The stale DOL XML retrieval path is labeled `Source health` and `UNKNOWN`, while the accepted PDF-backed current observation remains available.

Current governed connector-signal count is zero because the persistent fixture contains zero accepted structural relationships. This prevents false coral/teal “impact” lighting. The connector glint period remains constant at 2,500 ms; hover changes opacity/emphasis, not speed.
