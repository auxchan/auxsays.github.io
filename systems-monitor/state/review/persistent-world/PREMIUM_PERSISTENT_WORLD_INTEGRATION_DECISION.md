# Premium Persistent World Integration Decision

Status: **IMPLEMENTED**

## Decision

Preserve `PersistentWorldReadModel` as the sole topology/coordinate authority and preserve `PersistentWorldShell` as navigation/history/accessibility authority. Integrate premium visuals in a new Canvas surface plus pure deterministic visual helpers.

## Reused/ported

- Canvas atmospheric gradient, structural grid, vignette and bounded particles;
- stable cubic connector treatment with outer glow/body/inner filament;
- fixed-period/fixed-trail glints;
- eased hover color inheritance;
- hierarchy-safe Trace emphasis without implying a factual causal direction;
- substantial focal and Level-1 shells;
- ten restrained driver glyphs;
- orbit guides, label plates and deterministic label collision suppression;
- camera, zoom, middle-pan, Reset and fullscreen composition.

## Rejected

- `layoutEmploymentOrbit`;
- `layoutSpatialNodes`;
- selection-keyed coordinate interpolation and replacement scene arrays;
- old two-sublayer topology;
- full-detail rendering or DOM controls for all 1,111 placements.

## Performance bounding

All placements and relationships remain resident. Rich shells, glows, multilayer rails, glints, icons and labels activate only for the bounded semantic LOD. Full-world uses density structure rather than 1,111 premium shells.

## Changed-file plan

- new `PremiumPersistentWorldSurface.tsx` Canvas controller/renderer;
- new `persistentWorldVisuals.ts` deterministic visual primitives;
- shell import seam and small CSS integration;
- visual invariant tests;
- investigation, benchmark and Human-QA evidence.

No taxonomy, topology, factual data, contract, Master, deployment, Patch Feed or BEA evidence changes are authorized or included.
