# Persistent World Integration Decision

Decision: **IMPLEMENT LOCAL PERSISTENT-WORLD FOUNDATION**
Renderer: **existing Canvas 2D with strict LOD**
Dependency change: **none**

## Architecture

```text
canonical factor registry
→ hierarchy placement registry
→ typed relationship registry
→ deterministic layout + graph snapshot fingerprint
→ Canvas density/focus renderer
→ camera, LOD, emphasis, inspector and accessible exact-ten list
```

The previous radial loop came from translating ten Labor Market measurements
into one star, overwriting coordinates with `layoutEmploymentOrbit`, and using
selection-dependent visibility/layout keys. The new R&D model establishes a
single immutable authority with 1,111 placements and 3,110 relationships.

## Gate result

| Gate | Result |
|---|---|
| No unresolved BINDING contradiction | PASS WITH LOD CONSTRAINT |
| Registry/layout boundaries clear | PASS |
| Renderer choice benchmarked | PASS WITH STRICT LOD |
| Fixture/factual isolation | PASS |
| No fabricated factual relationships | PASS |

## Implementation boundary

- Outcome plus the ten Master driver systems are semantic at overview.
- 100 Level-2 labels are review candidates.
- 1,000 Level-3 records are unmistakable deterministic renderer fixtures.
- All IDs, relationships, and coordinates remain resident and unchanged.
- Full-world view is an explicit density LOD, not 1,111 labels/controls.
- Selection changes only camera/LOD/emphasis/inspector/history.
- The six factual Labor Market readings stay in their original factual shell.
- No accepted structural relationship, Gate-B evidence, forecast, activation,
  or deployment is created.

Public persistent-graph output remains a later additive PDI/review task.
